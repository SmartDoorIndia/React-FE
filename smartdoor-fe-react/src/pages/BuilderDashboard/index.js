/** @format */

import React from 'react';

const BuilderDashboardLayout = React.lazy(() => import('./BuilderDashboardLayout'));

const BuilderDashboard = () => {
  return <BuilderDashboardLayout />;
};

export default BuilderDashboard;
